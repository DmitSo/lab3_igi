@model List<lab1_ef.ServiceType>

<div><img src="~/images/logo.jpg" /></div>
@using (Html.BeginForm())
{
    <h2>AVAILABLE SERVICES</h2>
    <div>
        <ul>
            <li><p>ID, Name, Price</p></li>
            @foreach (var type in Model)
            {
                <li>
                    <p>@type.ServiceTypeId, @type.Name, @type.Cost</p>
                </li>
            }
        </ul>
    </div>
}
